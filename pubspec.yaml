name: flex_table
description: 'A new Flutter project.'
publish_to: 'none'

version: 0.9.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  multi_split_view: ^3.2.2
  popover: ^0.3.0+1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
