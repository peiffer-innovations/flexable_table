name: table_example
description: 'A new Flutter project.'
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  csv: ^6.0.0
  english_words: ^4.0.0
  flutter:
    sdk: flutter
  flexible_table:
    path: ../

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/ev_data.csv
    - assets/iou_zipcodes.csv
